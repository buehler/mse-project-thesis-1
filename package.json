{
  "name": "mse-project-thesis-1",
  "private": true,
  "scripts": {
    "prestart": "rm -rf public && mkdir public",
    "start": "concurrently -k npm:nodemon npm:server",
    "nodemon": "nodemon -e md,bib,yaml,png -w sections -w images --exec \"bash ./build.sh\"",
    "server": "http-server public -o"
  },
  "dependencies": {
    "concurrently": "^6.0.0",
    "http-server": "^0.12.3",
    "nodemon": "^2.0.7"
  }
}
